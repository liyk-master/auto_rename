# 核心依赖 - Python 3.9兼容版本
requests>=2.28.0
watchdog>=3.0.0
configparser>=5.3.0
colorama>=0.4.6
jinja2>=3.0.0
tqdm>=4.60.0

# HTTP客户端 - Python 3.9兼容版本
httpx>=0.24.0

# 数据库依赖 - 用于 emya 入库功能
SQLAlchemy>=2.0.0
PyMySQL>=1.1.0

# 可选依赖 - 注释掉需要Python 3.12的包
p123client  # 需要Python 3.12，暂时注释

# 开发依赖
pytest>=7.0.0
pytest-cov>=4.0.0

# 打包工具
pyinstaller>=5.0.0
nuitka>=1.0.0
setuptools>=60.0.0
wheel>=0.40.0